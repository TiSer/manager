<h1>Bill for milestone "<%= link_to @milestone.name, @milestone %>" </h1>

<h3>Interval between: (<%= @milestone.start_date %>) - (<%= @milestone.end_date %>).</h3>

  <table>
    <tr>
      <th>Employee</th>
      <th>Booking date</th>
      <th>Booking hours</th>
      <th>Activity cost</th>
      <th>hours*cost</th>
    </tr>
    <tr>
      <% bill.each do |bill| %>
        <% @employee = bill[0] %>
        <% @date = bill[1] %>
        <% @hours = bill[2] %>
        <% @cost = bill[3] %>
        <td> <%= @employee %>      </td>
        <td> <%= @date %>      </td>
        <td> <%= @hours %>      </td>
        <td> <%= @cost %>      </td>
        <td> <%= @hours * @cost %> </td>
    </tr>
      <% end %>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
      <td> <b>Sum:</b> <%= @summ %>. </td>
  </table>

<br/>
<%= link_to "Back", :back %>
